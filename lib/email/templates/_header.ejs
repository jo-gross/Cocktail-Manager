<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title : 'Cocktail-Manager' %></title>
  <meta name="color-scheme" content="light dark">
  <meta name="supported-color-schemes" content="light dark">
  <style>
    :root { color-scheme: light dark; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.5; color: #333; max-width: 600px; margin: 0 auto; padding: 24px; }
    .content { background: #f5f5f5; border-radius: 8px; padding: 20px; margin-bottom: 24px; }
    .button { display: inline-block; background: #2563eb; color: #fff !important; text-decoration: none; padding: 12px 20px; border-radius: 6px; margin-top: 12px; }
    .footer-divider { border: 0; border-top: 1px solid #ccc; margin: 24px 0 20px 0; }
    .footer-brand { display: flex; align-items: center; gap: 12px; margin-bottom: 16px; }
    .footer-brand a { text-decoration: none; color: #555; display: flex; align-items: center; gap: 12px; }
    .footer-brand img { width: 48px; height: 48px; flex-shrink: 0; display: block; }
    .footer-brand-text { display: flex; flex-direction: column; justify-content: center; height: 48px; box-sizing: border-box; }
    .footer-brand-name { display: block; font-size: 20px; line-height: 1.35; font-weight: 700; color: #555; }
    .footer-brand-tagline { display: block; font-size: 11px; line-height: 1.35; color: #777; margin-top: 2px; }
    .footer-legal { font-size: 9px; color: #888; line-height: 1.45; max-width: 520px; }
    .footer-legal a { color: #888; text-decoration: underline; }
    @media (prefers-color-scheme: dark) {
      body { color: #e5e5e5; }
      .content { background: #2d2d2d; color: #e5e5e5; }
      .button { background: #3b82f6; color: #fff !important; }
      .footer-divider { border-top-color: #444; }
      .footer-brand a { color: #b0b0b0; }
      .footer-brand-name { color: #b0b0b0; }
      .footer-brand-tagline { color: #909090; }
      .footer-legal { color: #808080; }
      .footer-legal a { color: #a0a0a0; }
    }
  </style>
</head>
<body>
  <div class="content">
